function(add_chhttp_example target source)
  add_executable(${target} ${source})
  target_link_libraries(${target} PRIVATE chhttp::chhttp)
  target_compile_features(${target} PRIVATE cxx_std_20)
  if(MSVC)
    target_compile_options(${target} PRIVATE /W4 /permissive- /utf-8)
  else()
    target_compile_options(${target} PRIVATE -Wall -Wextra -Wpedantic)
  endif()
endfunction()

# Keep the original target name for users who already build chhttp_example.
add_chhttp_example(chhttp_example example.cpp)
add_chhttp_example(chhttp_sync_client sync_client.cpp)
add_chhttp_example(chhttp_async_concurrency async_concurrency.cpp)
add_chhttp_example(chhttp_rest_server rest_server.cpp)
add_chhttp_example(chhttp_streaming streaming.cpp)
add_chhttp_example(chhttp_sse sse.cpp)
add_chhttp_example(chhttp_websocket websocket.cpp)
add_chhttp_example(chhttp_multipart multipart.cpp)
add_chhttp_example(chhttp_static_files static_files.cpp)
add_chhttp_example(chhttp_middleware middleware.cpp)
add_chhttp_example(chhttp_proxy proxy.cpp)
add_chhttp_example(chhttp_https https.cpp)
add_chhttp_example(chhttp_cancellation cancellation.cpp)
add_chhttp_example(chhttp_post_json_sse post_json_sse.cpp)
add_chhttp_example(chhttp_agent_tool_loop agent_tool_loop.cpp)
add_chhttp_example(chhttp_agent_parallel_fanout agent_parallel_fanout.cpp)
add_chhttp_example(chhttp_agent_stream_recovery agent_stream_recovery.cpp)
add_chhttp_example(chhttp_agent_backpressure agent_backpressure.cpp)
add_chhttp_example(chhttp_agent_stream_upload agent_stream_upload.cpp)
add_chhttp_example(chhttp_agent_vision_json agent_vision_json.cpp)
add_chhttp_example(chhttp_agent_multipart_stream agent_multipart_stream.cpp)
